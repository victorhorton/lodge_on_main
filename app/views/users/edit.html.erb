<div class="col bg-primary p-3 mt-3">
  <h1>Edit User</h1>
  <%= form_for @user do |f| %>
    <%= render(partial: 'form', locals: { f: f } ) %>
    <%= f.submit "Update", class: 'btn btn-secondary m-2' %>
  <% end %>
</div>